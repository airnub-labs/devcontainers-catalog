{
  "name": "@airnub/devcontainers-catalog-generator",
  "version": "0.1.0",
  "private": false,
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "catalog-generate": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node ./scripts/copy-sidecars.mjs",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test tests/**/*.test.mjs"
  },
  "dependencies": {
    "@airnub/devcontainers-catalog-manifest": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.2"
  }
}
