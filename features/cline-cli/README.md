# Cline CLI Feature

Installs the [Cline](https://github.com/cline/cline) command line interface globally inside the dev container using `pnpm`.

## Example Usage

```json
{
  "features": {
    "ghcr.io/airnub-labs/devcontainer-features/cline-cli:1": {}
  }
}
```
