{
  "version": "1.0.0",
  "id": "classroom-studio-webtop",
  "name": "Classroom Studio (Webtop)",
  "description": "Headless dev container plus a linuxserver/webtop sidecar (served over noVNC at http://localhost:{{templateOption.webtopPort}}).",
  "documentationURL": "https://github.com/airnub-labs/devcontainers-catalog",
  "publisher": "airnub-labs",
  "options": {
    "policyMode": {
      "type": "string",
      "enum": ["none", "managed"],
      "default": "none",
      "description": "Select built-in Chrome policy presets applied to the sidecar."
    },
    "webtopPort": {
      "type": "integer",
      "default": 3000,
      "description": "Forwarded port for the desktop (webtop) service."
    },
    "chromePolicies": {
      "type": "string",
      "default": ".devcontainer/policies/managed.json",
      "description": "Path to the Chrome policy file mounted into the webtop container. Leave as-is to follow the selected policy mode."
    }
  }
}
