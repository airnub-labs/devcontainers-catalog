# Supabase classroom defaults — copy to .env and replace secrets before docker compose up.
POSTGRES_PASSWORD=supabase
PGRST_DB_URI=postgres://postgres:supabase@supabase-db:5432/postgres
PGRST_DB_ANON_ROLE=anon
DB_HOST=supabase-db
STUDIO_PG_META_URL=http://supabase-rest:3000
# Provide real JWT secrets/service role keys via Codespaces Secrets or a local .env override.
# SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
