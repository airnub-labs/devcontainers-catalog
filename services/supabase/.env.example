# Copy to .env and update before running `docker compose` if you need non-default credentials.
POSTGRES_PASSWORD=supabase
PGRST_DB_ANON_ROLE=anon
# Optional overrides; defaults assume the compose stack network names.
# PGRST_DB_URI=postgres://postgres:${POSTGRES_PASSWORD}@supabase-db:5432/postgres
# DB_HOST=supabase-db
# STUDIO_PG_META_URL=http://supabase-rest:3000
