# Prefect classroom overrides — copy to .env if you expose the API beyond the container network.
PREFECT_ORION_API_HOST=0.0.0.0
PREFECT_API_URL=http://prefect:4200/api
# Uncomment to require authentication for the UI/API.
# PREFECT_API_KEY=
