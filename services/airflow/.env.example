# Airflow classroom defaults — copy to .env before launching docker compose.
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASS=admin
# Optionally override the database connection if you host Postgres elsewhere.
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@airflow-db:5432/airflow
# Disable tutorial DAGs to keep the UI focused on lesson content.
AIRFLOW__CORE__LOAD_EXAMPLES=false
