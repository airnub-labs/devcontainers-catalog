# Default admin credentials for demos only — change in production
AIRFLOW_USERNAME=admin
AIRFLOW_PASSWORD=admin
# Optional Fernet key placeholder (generate a secure value before production use)
AIRFLOW__CORE__FERNET_KEY=replace-with-32-byte-key
