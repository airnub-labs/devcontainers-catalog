# Temporal CLI uses the gRPC endpoint exposed by temporal:7233 inside the devnet network
TEMPORAL_CLI_ADDRESS=temporal:7233
# Optionally surface the UI externally via this port mapping override
TEMPORAL_UI_PORT=8081
