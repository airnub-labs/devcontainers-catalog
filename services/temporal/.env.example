# Temporal classroom defaults — copy to .env to relax or tighten CORS/credentials.
TEMPORAL_ADDRESS=temporal:7233
TEMPORAL_CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:4200
# Provide an auth token when pairing with a hosted Temporal Cloud project.
# TEMPORAL_API_KEY=
